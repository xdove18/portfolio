# Handoff: Агентство Pro — CRM для риелторов

## Обзор

CRM для агентства недвижимости. Главная задача продукта: ни один клиент не получает
повторный звонок от второго риелтора, потому что вся история касаний (звонки, показы,
сообщения, встречи) видна всей команде в одной ленте.

В пакете: рабочий десктоп-прототип со всеми разделами и обучением, три анимации
(таймлапс создания клиента, обзор разделов, мобильные экраны) и анимированная
заставка кейса 457,5 × 210.

## О файлах в пакете

Файлы в `prototypes/` — **дизайн-референсы, сделанные на HTML**. Это прототипы,
показывающие внешний вид и поведение, а не продакшн-код для копирования.
Задача: **воспроизвести эти экраны в существующем окружении кодовой базы**
(React, Vue, Svelte, native — что уже используется), опираясь на её паттерны и
библиотеки. Если окружения ещё нет, выбрать подходящий фреймворк и реализовать там.

Файлы `.dc.html` открываются прямо в браузере (нужен лежащий рядом `support.js`).
`.jsx` — анимационные сцены, они подключаются из соответствующих `.dc.html`.

## Точность

**High-fidelity.** Цвета, типографика, отступы, радиусы и состояния финальные —
воспроизводить пиксель в пиксель, подставляя компоненты своей системы.
Исключение: анимации — это раскадровки того, как ведёт себя интерфейс, а не
требования к реализации motion-слоя.

---

## Дизайн-токены

### Цвета (тёмная тема — основная)

| Токен | Значение | Где |
|---|---|---|
| `bg` | `#0B0B0A` | фон приложения |
| `panel` | `#131312` | карточки, модальные окна |
| `bg2` | `#1C1B19` | поля ввода, вторичные кнопки, плашки событий |
| `bg3` | `#26241F` | выбранная строка, аватар в шапке, третичные кнопки |
| `tx1` | `#F5F3F0` | основной текст |
| `tx2` | `#BCB8B2` | вторичный текст |
| `tx3` | `#8B8680` | подписи, мета, плейсхолдеры |
| `hair` | `rgba(255,255,255,.08)` | разделители, границы |
| `bd2` | `#4A4540` | акцентная левая граница заметки |
| `cta` | `#27C08B` | акцент, основные кнопки, активные состояния |
| `pillTx` | `#08211A` | текст на кнопке `cta` |

Светлая тема есть в прототипе (переключатель Light/Dark вверху страницы):
`bg #F7F5F2`, `panel #FFFFFF`, `bg2 #F0EDE8`, `bg3 #E5E1DA`, `tx1 #16150F`,
`tx2 #4A463F`, `tx3 #8B8680`, `hair rgba(20,18,14,.10)`, `cta #17916A`,
`pillTx #FFFFFF`.

### Статусы клиента и типы касаний

| Смысл | Токен | Hex |
|---|---|---|
| Активен | `stA` | `#6FC28A` |
| Жду звонка | `stW` | `#D2A65B` |
| Холодный | `stC` | `#A5A09A` |
| Отказал | `stR` | `#D4837A` |
| Показ | `home` | `#6FC2A0` |
| Звонок | `phone` | `#7FA8D4` |
| Сообщение | `msg` | `#D2B36B` |
| Встреча | `meet` | `#B99AD4` |

Цвет статуса используется точкой 7 × 7 и цветом подписи — без заливок и обводок
всей строки (обводки статусов из ранних версий убраны намеренно).

### Типографика

- Основной шрифт: **Inter** (400 / 500 / 600 / 700), fallback `-apple-system, "SF Pro Text", "Segoe UI", sans-serif`.
- Цифры и метки: **JetBrains Mono** (400 / 500) — суммы, время, даты, счётчики, надзаголовки.
- Надзаголовок (label): 10–11px, `letter-spacing: .16em`, `text-transform: uppercase`, `tx3`.
- Заголовок раздела: 26px / 600 / `letter-spacing: -.02em`.
- Имя клиента в карточке: 28px / 600 / `-.02em`.
- Заголовок блока: 20px / 600 / `-.01em`.
- Основной текст: 13–14px, `line-height: 1.6`.
- Мета: 11–12px, `tx3`.
- `font-feature-settings: "tnum" 1` на body — цифры не «прыгают».

### Размеры и отступы

- Скругления: 10 (иконки в шапке), 12–14 (поля, строки), 16–18 (карточки), 20–22 (окна), 999 (кнопки, чипы).
- Шаг отступов: 4 / 6 / 8 / 10 / 12 / 14 / 16 / 18 / 20 / 24 / 28.
- Высоты: шапка 60, строка меню 38, кнопка-пилюля 36 (десктоп) и 44–48 (мобилка), поле ввода 38–48.
- Тени: окно `0 24px 80px rgba(0,0,0,.5)`, модальное окно `0 40px 100px rgba(0,0,0,.6)`, шторка на мобилке `0 -18px 44px rgba(0,0,0,.45)`.
- Свечение: `radial-gradient(circle, rgba(39,192,139,.16–.20), transparent 68–70%)`, эллипс ~720 × 520 в левом верхнем углу **внутри** окна приложения.

---

## Экраны

### 1. Главный экран (desktop, 1440 × 900)

Три колонки внутри окна со скруглением 20 и тонкой границей `hair`.

**Верхняя панель (высота 60, нижняя граница `hair`).**
Слева: логотип «АГЕНТСТВО» 15px / 700 / `.04em` с точкой цвета `cta`, вертикальный
разделитель 1 × 18, подпись раздела 12px `tx3`. Справа: два счётчика
(`26 клиентов`, `14 показов за неделю` — число моноширинным 15px, второе цветом
`cta`), разделитель, две иконки 34 × 34 (фильтры, уведомления) без обводки, затем
имя пользователя (12px / 600) с ролью «Риелтор» (11px `tx3`) и аватар 32 × 32,
радиус 10, фон `bg3`.

**Меню (ширина 212, правая граница `hair`, padding 16 / 12).**
Надзаголовок «МЕНЮ». Пункты: Клиенты 26, Показы 14, Сделки 4, Дашборд, Отчёты,
Команда 6. Строка: высота 38, радиус 10, точка 6 × 6 слева (`cta` у активного,
`bd2` у остальных), лейбл, счётчик моноширинным 11px `tx3`. Активный пункт — фон
`bg2`, текст `tx1`, вес 600. Внизу колонки карточка `bg2`, радиус 16: заголовок
«Ни одного повторного звонка» 14 / 600, текст 12px `tx2` и кнопка-пилюля
«Как это работает» (высота 32, прозрачный фон, граница `hair`) — открывает обучение.

**Список клиентов (ширина 296, правая граница `hair`).**
Заголовок «Клиенты» 20 / 600 + иконка фильтров 28 × 28. Поиск: высота 38, радиус 999,
фон `bg2`, иконка лупы слева, плейсхолдер «Поиск по имени, телефону...».
Кнопка «+ Новый клиент»: высота 36, радиус 999, фон `cta`, текст `pillTx` 12 / 600.
Строка клиента: высота 68, радиус 14, padding 12, аватар-круг 36 с инициалами,
имя 14 / 600, под ним точка статуса + подпись статуса цветом статуса + «· вчера»
11px `tx3`; справа бюджет моноширинным 12px и спарклайн из 6 столбиков шириной 3
(серый `bg3` = день без касаний, `cta` с нарастающей непрозрачностью = касания).
Выбранная строка — фон `bg3`, без обводки.

**Карточка клиента (оставшаяся ширина).**
Шапка: аватар 56, имя 28 / 600, статус точкой и подписью, мета «Создан 10 сентября ·
ведёт Марина Грязнова» 12px `tx3`; справа кнопки «Позвонить» (`cta`, с иконкой
телефона) и «WhatsApp» (`bg2`), высота 36, радиус 999.
Метрики: четыре карточки `panel`, радиус 16, padding 14 / 16 — надзаголовок 11px
uppercase `tx3` и значение моноширинным 22px с единицей 11px `tx3`.
История взаимодействий: карточка `panel`, радиус 18. В шапке — надзаголовок,
счётчик «5 событий» цветом `cta`, спарклайн за 14 дней, переключатель
«Все / Показы / Звонки / Сообщения» (пилюли высотой 26 в контейнере `bg2`).
Внизу кнопка «+ Добавить событие» (высота 44, `cta`) и подпись
«30 секунд на запись — и коллеги в курсе» 11px `tx3`.

### 2. История взаимодействий

Группировка по дням: слева колонка 96px с днём (13 / 600) и датой (моно 11px `tx3`),
справа лента, отделённая вертикальной линией `hair`, на линии — точка 7 × 7 цвета типа
касания напротив каждого события.

Событие — плашка `bg2`, радиус 14, padding 12 / 14: иконка типа 16px цветом типа,
заголовок 14 / 600, мета (время моно 11px, автор 12px) и, если заметки есть, метка
с иконкой закладки и счётчиком: «1 заметка» / «2 заметки».

Заметки внутри события (отступ слева 36):
- аватар-квадрат 20 × 20, радиус 6, фон `bg3`, инициалы моно 9px;
- имя автора 13 / 600, время моно 11px `tx3`;
- текст 13px, `line-height: 1.6`;
- действия «Изменить» и «Удалить» — 12px `tx3`;
- в конце ветки — «+ Добавить заметку» 12 / 600 цветом `cta`.

Если заметок нет, у события показывается только «+ Добавить заметку» — событие с
заметкой и событие без заметки должны читаться по-разному с одного взгляда.

### 3. Показы, Сделки, Дашборд, Отчёты, Команда

- **Показы** — таблица с колонками `120px 1fr 180px 170px 130px`: дата (моно),
  объект (адрес 14 / 500 + цена моно 11px `tx3`), клиент, риелтор, итог
  (вертикальная полоска 3 × 14 + подпись цветом итога: Запланирован, Думает,
  Понравилось, Отказ).
- **Сделки** — три колонки-этапа (Подбор, Оформление, Закрыты) с точкой цвета этапа,
  суммой и строками сделок, разделёнными линиями `hair`. Без карточек-«канбан-плашек».
- **Дашборд** — четыре метрики с дельтой, график касаний по дням (площадная кривая
  `cta` с градиентной заливкой от `rgba(39,192,139,.28)` к прозрачному, точки на
  каждом дне, увеличенная точка на выбранном дне, под графиком значения и дни недели)
  и список «Показы по риелторам» с полосами прогресса высотой 6.
- **Отчёты** — четыре карточки `panel` с названием, периодом (моно `tx3`), описанием
  и кнопками «Скачать» (`cta`) и «Ссылка» (`bg3`).
- **Команда** — строки `1fr 110px 110px 200px`: человек с аватаром и ролью, число
  клиентов, число показов, полоса нагрузки (`cta`; при нагрузке > 85% — `msg`).

### 4. Обучение

Обучение работает **точечными подсказками по ходу действия**, а не одним большим
модальным окном. Шаги:

1. «Начните с клиента» — подсвечена первая строка списка;
2. «Звонок из карточки» — подсвечены кнопки «Позвонить / WhatsApp»;
3. «Запишите касание» — подсвечена кнопка «+ Добавить событие»;
4. «Кто остыл» — подсвечена иконка фильтров в шапке.

Подсветка: рамка 2px цвета `cta` вокруг реального элемента с отступом 7px и
затемнением остального экрана через `box-shadow: 0 0 0 9999px rgba(6,6,5,.66)`.
Рамка должна считаться от фактических координат элемента (в React — `ref` +
`getBoundingClientRect`), а не задаваться константами, иначе она «съезжает» при
изменении вёрстки.

Подсказка рядом с элементом: карточка 274px, `panel`, радиус 14, тень
`0 22px 54px rgba(0,0,0,.6)`; внутри — счётчик «2 / 4» моно 10px цветом `cta`,
крестик, заголовок 14 / 600, текст 12px `tx2`, точки-прогресс (активная — пилюля
22 × 7 цветом `cta`) и кнопки «Назад» (`bg2`) / «Далее» (`cta`) высотой 28.

### 5. Добавление события (модальное окно)

Окно 600 × 500, радиус 20, фон `panel`, подложка `rgba(6,6,5,.62)`. Никаких обводок
у полей — только фон `bg2`, радиус 12–14, высота 40–48.

Поля: клиент и дата (только чтение), тип события чипами
(Показ квартиры, Звонок, WhatsApp сообщение, Встреча в офисе, Email, Другое),
контекстное поле (адрес объекта для показа, длительность для звонка), заметка,
статус клиента чипами с точкой цвета статуса. Выбранный чип типа — фон `cta`,
текст `pillTx`; выбранный чип статуса — фон `color-mix(in srgb, <цвет статуса> 22%, panel)`
и текст цвета статуса. Внизу «Сохранить» (`cta`) и «Отменить» (`bg2`), обе — 44–48 / 999.

### 6. Мобильная версия (390 × 844)

Та же палитра и та же плотность, что на десктопе.

- Статус-бар 48, шапка 56: слева бургер или «назад», по центру заголовок, справа
  фильтры или «ещё». Все зоны нажатия ≥ 44px.
- Список клиентов: два сводных чипа, поиск (44 / 999), чипы статусов, карточки
  клиентов `panel` (выбранная — `bg3`) с аватаром, статусом, суммой и спарклайном.
- Навигация между разделами — **через шторку меню снизу**: бургер → шторка
  (радиус 24 сверху, «ручка» 36 × 4, заголовок, шесть пунктов по 48) → выбор пункта.
- Фильтры — такая же шторка: группы «Статус» и «Срок контакта» с чекбоксами 18 × 18
  (отмеченный — заливка `cta`), внизу «Применить» и «Сбросить».
- Карточка клиента: аватар 64, имя 20 / 600, статус, две кнопки в ряд, сетка 2 × 2
  метрик, история с заметками, закреплённая внизу кнопка «+ Добавить событие».
- Форма события: компактная строка «клиент + дата» с аватаром, чипы типа, поле
  адреса, чипы статуса, заметка (высота 104) и закреплённый футер с двумя кнопками;
  тело формы скроллится, футер не обрезается.
- Переходы: вглубь (карточка, форма) — сдвиг слева направо с тенью, как push-навигация;
  между разделами — мягкое появление сверху вниз, без одновременного показа двух экранов.

### 7. Заставка кейса (457,5 × 210)

Тёмный фон `#0F100E`, два «дышащих» радиальных свечения (зелёное и голубое),
белый круг 52 с иконкой дома, заголовок «Агентство Pro» 26 / 700, столбики касаний
(зелёные, растут по очереди) и белая плашка-уведомление внизу: аватар 26, заголовок
13 / 600, подпись 11px `#8B8680`, время «сейчас». Две плашки сменяют друг друга
циклом 7 секунд. Реализовано на CSS-анимациях, без JS.

---

## Взаимодействия и поведение

- Клик по пункту меню переключает раздел; активный пункт подсвечивается.
- Клик по строке клиента открывает его карточку справа (на мобилке — отдельный экран).
- Поиск фильтрует список по имени и телефону; при пустом результате — «Клиентов не найдено».
- Фильтры: статус клиента и срок последнего контакта (в том числе «не было касаний 7 дней»).
- «+ Добавить событие» открывает модальное окно; после сохранения событие появляется
  в истории, счётчики в шапке и меню пересчитываются, статус клиента обновляется.
- Заметки: добавление, редактирование и удаление внутри события; у события может быть
  несколько заметок от разных риелторов.
- Обучение запускается кнопкой «Как это работает» в меню, шаги переключаются
  «Далее / Назад», закрывается крестиком.
- Анимации появления: карточки и строки поднимаются на 26px с `opacity 0 → 1`,
  `ease-out-cubic`, длительность ~550ms, задержка между соседними элементами 70–120ms.

## Состояние

- `page` — активный раздел.
- `clients[]`, `selectedClientId`, `query`, `filters { statuses[], lastContact }`.
- `events[]` у клиента: `{ id, kind, title, datetime, authorId, notes[] }`.
- `notes[]` внутри события: `{ id, authorId, datetime, text }`.
- `eventModal { open, kind, fields, status }`.
- `tour { open, step }`.
- `theme` — light / dark.
- Мобильная версия дополнительно: `sheet` (`none | menu | filters`), стек навигации.

## Ассеты

Все иконки — инлайновые SVG 24 × 24, `stroke: currentColor`, `stroke-width: 1.7–1.8`,
скруглённые концы: дом (показ), телефон (звонок), облако сообщения, календарь (встреча),
лупа, фильтр, колокольчик, закладка (заметка), стрелка «назад», бургер, «ещё».
Растровых изображений и сторонних иконочных наборов нет. Шрифты — Google Fonts:
Inter и JetBrains Mono.

## Файлы

| Файл | Что внутри |
|---|---|
| `prototypes/Агентство Pro CRM v2.dc.html` | Основной интерактивный прототип: все разделы, обучение, модальные окна, светлая и тёмная темы, десктоп и мобильные экраны |
| `prototypes/Таймлапс - клиент Сергей Грин.dc.html` + `crm-timelapse.jsx` | Анимация: риелтор заводит клиента и ведёт историю касаний |
| `prototypes/Обзор разделов + обучение.dc.html` + `crm-tour.jsx` | Анимация: обучение подсказками и обзор всех разделов |
| `prototypes/Мобильные экраны.dc.html` + `crm-mobile.jsx` | Анимация: работа с мобильной версией, навигация через шторку меню |
| `prototypes/Заставка кейса 457x210.dc.html` | Анимированная обложка кейса для сайта |
| `prototypes/animations-v3.jsx`, `prototypes/support.js` | Рантайм, нужный прототипам для запуска |
